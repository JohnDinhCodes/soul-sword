class PlayScene {
    constructor(dialogue, window) {
        this.dialogue = dialogue;
        this.window = window;
    }

    test() {
        this.window.addEventListener("click", () => {
            
        })
    }
}